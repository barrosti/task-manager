import { TaskFinishedDirective } from './task-finished.directive';

describe('TaskFinishedDirective', () => {
/*	
  it('should create an instance', () => {
    const directive = new TaskFinishedDirective();
    expect(directive).toBeTruthy();
  });
  */
});
